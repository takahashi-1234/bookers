<h1>Editing Book</h1>
<% if @book.errors.any? %>
    <div class="error">
        <div class="error_message">
            <%= @book.errors.count %>errors prohibited this book from being saved
        </div>
        <div class="error_box">
            <% @book.errors.full_messages.each do |message| %>
            <p>・<span><%= message %></span></p>
        <% end %>
        </div>
    </div>
<% end %>

<%= form_with model:@book,url:"/books/#{@book.id}" ,local:true do |f| %>

<div class="field">
    <p>Title</p>
    <%= f.text_field:title %>
</div>
<div class="field">
    <p>Body</p>
    <%= f.text_area:body %>
</div>
<div class="field">
    <p><%= f.submit "Update Book" %></p>
</div>

<div class=btn_sub>
<p class="btn"><%= link_to"Show" ,book_path(@book) %></p>
<p>|</p>
<p class="btn"><%= link_to"Back" ,"/books" %></p>
</div>
<% end %>